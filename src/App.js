import React from 'react';
import TweetBox from './tweet_box';

var App = React.createClass({

  render: function() {

    return (
      <div>
        <TweetBox />
      </div>
    );
  }
});

export default App;
